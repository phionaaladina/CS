#qn1 a) python program to calculate the area of a triangle.

#Area = 0.5 * base * height

base = float(input('Base of triangle: '))

height = float(input('Height of triangle: '))

area =  0.5 * base * height

print(f'The area of the triangle with base {base} cm and height {height}cm  is {area} cm')

